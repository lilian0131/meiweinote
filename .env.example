# JWT 密钥（生产环境必须修改）
JWT_SECRET=your-secret-key-change-this-in-production

# Vercel Postgres 连接字符串（部署到 Vercel 后自动添加）
# 本地开发时可以设置为本地 PostgreSQL 连接字符串
# POSTGRES_URL=postgresql://user:password@localhost:5432/meiweinote
